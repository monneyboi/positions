# Copy to .env and fill in. Register an owner-only OAuth 2.0 consumer at
# https://meta.wikimedia.org/wiki/Special:OAuthConsumerRegistration
# Only the access token is used at runtime (sent as a Bearer token).
WIKIDATA_CLIENT_KEY=
WIKIDATA_CLIENT_SECRET=
WIKIDATA_ACCESS_TOKEN=
